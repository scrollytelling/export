Use this file to create an `index.json` for each Scrollytelling account.

To run it:

``` shell
# in a folder that will become the web root:
${scrolly_home}bin/rails runner ${export_home}/entries.rb
```
